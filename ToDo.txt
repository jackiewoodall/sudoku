Sudoku solver tasks:

o be able to load a puzzle from a string into model

o what's the relationship between the Model (js), View (html), and Controller (js+jQuery)?

o when a new model is created, update the view
    - clear the UI board
    - set the proper elements of the UI board with the fixed numbers
    - disable the elements of the set UI elements
    - update all other cells with their possible candidates in the cells

o when item picked from UI, update the model which updates the ui
    - the form calls a js function?
    - through jQuery an event handler is registered?
        $("select").change( CallbackEventFunctionHandler );

o end of game detection

o begin puzzle creation

o edit mode/enter puzzle

o html() contents of a <select> element:

    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5">5</option>
    <option value="6">6</option>
    <option value="7">7</option>
    <option value="8">8</option>
    <option value="9">9</option>

